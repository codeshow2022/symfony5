{% extends 'base.html.twig' %}
{% block title %}{{ data.address.name }}{% endblock %}

 {% block body %}
     <div class="row">
         <div class="col-12">
             {{ form_start(form) }}
             {% include 'address/_form-table.html.twig' %}
             <button type="submit" class="btn btn-primary">Save</button>
             {{ form_end(form) }}
         </div>
     </div>
 {% endblock %}
